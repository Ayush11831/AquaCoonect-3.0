# Root .env.example — used by docker-compose. Copy to .env and fill in.
# Optional: without it the ML service falls back to offline defaults.
OPENWEATHER_API_KEY=
# Change before any non-local deployment.
JWT_SECRET=change_me_in_production
