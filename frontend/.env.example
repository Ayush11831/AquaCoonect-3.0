# frontend/.env.example — copy to .env if you need to override defaults
# In dev, API calls proxy to http://localhost:3001 via package.json "proxy".
# In Docker, nginx proxies /api to the backend service.
